npm install chalk@5.0.1 --save--exact 
npm install chalk

$ node -v

--refresh paste on git--

git rm -r --cached .

git add .

git commit -m ".gitignore funcionando ;)"

--lib
npm install chalk@5.0.1 --save--exact

expressões regulares
[regex](https://regex101.com/)
\[[^\[\]]*?\]

const regex = /[a-zA-z\s]/;

let regex = new RegExp("[a-zA-z\s]");
